# Gradle
.gradle/
build/
!build-scripts/
local.properties
build-scripts/keystore.properties
*.jks
*.keystore
# ...except the development signing key, which is deliberately committed so
# every build produces an installable APK. It is not a secret - see
# build-scripts/README.md.
!build-scripts/dev-signing.jks

# IDE
.idea/
*.iml
.kotlin/
out/

# OS
.DS_Store
Thumbs.db

# Packaging output
dist/

# Generated by build-scripts/make-icns.sh for a local macOS build; only macOS
# tooling can build it, so it is produced on demand rather than committed.
assets/icon/app-icon.icns
