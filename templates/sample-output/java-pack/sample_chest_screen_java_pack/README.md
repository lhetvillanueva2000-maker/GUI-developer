# Sample Chest Screen - Java Edition export

Generated by **UILabs**.

## What is in this folder

| Path | Purpose |
| --- | --- |
| `pack.mcmeta` | Resource pack manifest. |
| `assets/mcgui/gui/sample_chest_screen.json` | Portable layout description (the source of truth). |
| `assets/mcgui/lang/en_us.json` | Translation keys for every label in the screen. |
| `assets/mcgui/textures/gui/` | Every texture you imported into the project. |
| `src/main/java/.../SampleChestScreen.java` | Ready-to-compile `Screen` subclass. |

## Using the resource pack half

Zip the contents of this folder (the folder that contains `pack.mcmeta`,
not the folder itself) and drop the zip into `.minecraft/resourcepacks`.
The textures become available as `mcgui:textures/gui/<file>.png`.

## Using the generated screen

1. Copy `SampleChestScreen.java` into your mod's source tree.
2. Adjust the package statement if your mod does not use
   `com.example.mcgui`.
3. Open it with `Minecraft.getInstance().setScreen(new SampleChestScreen());`

The class centres the 176x166
layout in the window, so it behaves correctly at every GUI scale.

## Regenerating

Re-export from the designer at any time. The generated Java is
overwritten wholesale, so keep custom logic in a subclass.